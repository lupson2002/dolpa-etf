# 돌파etf — 한/미 통합 ETF 저변동 모멘텀 돌파 감시 시스템
# =====================================================================

# 데이터 소스
yfinance>=0.2.40          # 야후 파이낸스 공식 스크리너(ETFQuery) + 미국 ETF 일봉/실시간
FinanceDataReader>=0.9.20 # 국내 ETF 리스트 + 일봉 + 실시간 현재가

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0

# 알림
requests>=2.31.0          # 텔레그램 봇 HTTP API 호출

# 이메일 (whitelist Excel 첨부 발송)
openpyxl>=3.1.0           # .xlsx 생성
# smtplib/email.message — 표준 라이브러리(별도 설치 불필요)

# 환경변수
python-dotenv>=1.0.0      # .env 로더 (텔레그램 토큰/이메일 앱 비밀번호)

# 워드클라우드 (화이트리스트 테마 분석)
wordcloud>=1.9.0          # ETF 이름 키워드 워드클라우드 PNG 생성

# 대시보드 UI 및 차트
streamlit>=1.30.0         # 인터랙티브 웹 대시보드
plotly>=5.18.0            # 캔들스틱 및 테마 인터랙티브 차트